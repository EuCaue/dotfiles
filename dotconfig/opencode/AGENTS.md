# MANDATORY FIRST ACTION

Before your first response in every session: call the `skill` tool to load `caveman` and `humanizer`. Then apply both to every response, every turn. This is not optional. (`ponytail` too, when available and not already active as plugin.)

# Workflow

- Don't explore the codebase beyond explicitly mentioned files, unless requested.
- If unsure about an API: write small scripts, print out info, make informed decisions.
- Read errors and logs completely before proposing a fix. No guessing.
